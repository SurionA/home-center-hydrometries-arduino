const home = require('./home');
const previsionmeteo = require('./previsionmeteo');
const openweathermap = require('./openweathermap');

module.exports = {
    home,
    openweathermap,
    previsionmeteo,
};
